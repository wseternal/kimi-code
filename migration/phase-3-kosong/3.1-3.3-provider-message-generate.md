# Phase 3: LLM Abstraction — kosong

## Source
- `packages/kosong/src/provider.ts` (275 lines)
- `packages/kosong/src/message.ts` (253 lines)
- `packages/kosong/src/tool.ts` (25 lines)
- `packages/kosong/src/usage.ts` (55 lines)

## Go Target
- `internal/kosong/provider.go` — ChatProvider interface, StreamedMessage, GenerateOptions, FinishReason, ThinkingEffort, ResponseFormat
- `internal/kosong/message.go` — Message, ContentPart, StreamedMessagePart, MergeInPlace, ExtractText, Create*Message helpers
- `internal/kosong/tool.go` — Tool struct
- `internal/kosong/usage.go` — TokenUsage with arithmetic helpers
- `internal/kosong/generate.go` — Generate() stream consumer, CollectParts()

## Decisions
- `<-chan StreamedMessagePart` replaces `AsyncIterable<StreamedMessagePart>`
- `context.Context` replaces `AbortSignal` for cancellation
- `StreamedMessage` is a struct with a Parts channel plus metadata fields (ID, Usage, FinishReason, etc.)
- ChatProvider interface uses methods (Name(), ModelName()) instead of readonly properties
- Generate returns `*StreamedMessage` (not channel directly) to carry metadata
- Provider adapters deferred to post-MVP (skeleton directories already created)

## Issues
- None

## Validation
- 10 tests passing: MergeInPlace (5 variants), Generate stream merge, CreateUserMessage, ExtractText, TokenUsage, IsToolDeclarationOnlyMessage

## Timestamp
2025-01-22T00:00:00Z
