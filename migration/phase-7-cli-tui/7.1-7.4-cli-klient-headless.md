# Phase 7: CLI & TUI

## Source
- `apps/kimi-code/src/main.ts`, `cli/commands.ts`, `cli/options.ts`
- `packages/klient/`, `packages/node-sdk/`

## Go Target
- `internal/cli/root.go` — CLI entry with subcommands (TUI, -p headless, server, version, help)
- `internal/klient/harness.go` — In-process engine facade for single-user CLI

## Decisions
- CLI uses native Go flag parsing instead of cobra (simpler, no external dep)
- Harness provides session CRUD + prompt submission without HTTP server
- TUI and headless modes stubbed (require provider adapter to be fully wired)

## Validation
- All 48 tests passing across all packages
- Binary builds successfully with `go build ./cmd/kimi`
